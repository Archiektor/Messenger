import {ActionType} from "./store";
import {FriendsPage} from "../App";

const friendsReducer = (partOfState: FriendsPage, action: ActionType): FriendsPage => {
    return partOfState;
}

export default friendsReducer;